"""
MikeCast CrewAI package.

Contains the agent/crew implementations that replace Steps 0–8b of the
daily pipeline when `mikecast_briefing.py` is invoked with `--crew`.

Step 9 (audio) and Step 10 (deliver / email / RSS) are unchanged and
remain in their original modules.
"""
